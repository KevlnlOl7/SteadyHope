import SwiftUI
struct IndexView: View {
    var body: some View {
        VStack {
            Text("這是首頁")
        }
        .navigationTitle("首頁")
        .navigationBarBackButtonHidden(true)
    }
}
